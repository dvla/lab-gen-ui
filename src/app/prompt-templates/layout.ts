import { Metadata } from 'next';
import PromptTemplate from './page';

export const metadata: Metadata = {
    title: 'Instant Productivity',
    description: 'Try a range of pre-defined templates such as summary, alliteration, or keywords',
};

export default PromptTemplate;
