import { Metadata } from 'next';
import Summary from './page';

export const metadata: Metadata = {
    title: 'Call Summary',
    description: 'Description of the Call Summary',
};

export default Summary;
