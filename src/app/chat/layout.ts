import { Metadata } from 'next';
import ChatPage from './page';

export const metadata: Metadata = {
    title: 'Chat',
    description: 'Experience the power of AI-driven conversation with our chatbot',
};

export default ChatPage;
