import { Metadata } from 'next';
import ModelInfo from './page';

export const metadata: Metadata = {
    title: 'Model Information',
    description: 'Find more about our models',
};

export default ModelInfo;